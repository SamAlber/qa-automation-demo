services:
  app:
    build:
      context: ./app
    container_name: flask_app
    image: flask-login-app
    ports:
      - "5001:5001"

  test:
    build:
      context: ./tests
    depends_on:
      - app
